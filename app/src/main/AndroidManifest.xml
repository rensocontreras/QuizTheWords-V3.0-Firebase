<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.contreras.myquizapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".View.NivelRankingActivity"></activity>
        <activity android:name=".Resultados.TimeOutBadActivity" />
        <activity android:name=".Resultados.BadActivity" />
        <activity android:name=".Resultados.GoodActivity" />
        <activity android:name=".Resultados.TimeOutGoodActivity" />
        <activity android:name=".View.RankingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.RegistrarActivity" />
        <activity android:name=".View.LoginActivity" />
        <activity android:name=".View.QuizActivity" />
        <activity android:name=".View.JugarActivity" />
        <activity android:name=".View.HomeActivity" />
    </application>

</manifest>